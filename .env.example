# Optional - defaults to public mainnet
SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# Optional - defaults to ~/.config/solana/id.json
SOLANA_KEYPAIR_PATH=~/.config/solana/id.json

# Optional - defaults to 3000
PORT=3000
